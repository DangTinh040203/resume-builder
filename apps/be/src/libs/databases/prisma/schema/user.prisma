model User {
  id         String   @id @default(uuid())
  providerId String
  provider   String
  firstName  String?
  lastName   String?
  email      String   @unique
  avatar     String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  resume Resume?

  @@map("users")
}
