generator client {
  provider        = "prisma-client"
  output          = "../generated"
  moduleFormat    = "cjs"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
}
